
Here is the combined installer script based on the provided content:

```python
from setuptools import setup, find_packages

setup(
    name='gpt_processor',
    version='1.0.0',
    description='A versatile tool to process text files using OpenAI ChatGPT API.',
    author='Your Name',
    author_email='your.email@example.com',
    packages=find_packages(),
    install_requires=[
        'openai',
        'PyYAML',
        # Add other dependencies here
    ],
    entry_points={
        'console_scripts': [
            'gpt-processer=gpt_processor.cli:main',
        ],
    },
    classifiers=[
        'Programming Language :: Python :: 3',
        'Operating System :: Microsoft :: Windows',
    ],
    python_requires='>=3.6',
)
```

### Explanation of the `setup.py` Components
- **`name`**: The name of your package. Users will refer to it as `gpt_processor` when installing.
- **`version`**: Version number of your package. Start with `1.0.0` and increment as you release updates.
- **`description`**: A short description of what your tool does.
- **`author` & `author_email`**: Your name and contact information.
- **`packages`**: Automatically finds all packages and subpackages within the `gpt_processor/` directory.
- **`install_requires`**: Lists all external Python dependencies required by your project.
- **`entry_points`**: Defines console scripts that `setuptools` will create. Here, it creates a command-line command named `gpt-processer` that points to the `main` function in `gpt_processor.cli` module.
- **`classifiers`**: Metadata about your package, such as supported Python versions and operating systems.
- **`python_requires`**: Specifies the Python versions compatible with your package.

### Preparing the `cli.py` Module
The `cli.py` module serves as the entry point for your command-line interface.

```python
import argparse
from .prompt_manager import PromptManager
from .file_handler import FileHandler
from .api_client import APIClient
from .config import Config
from .logger import setup_logger
from .utils import ensure_directory

def main():
    parser = argparse.ArgumentParser(description='ChatGPT Prompt Processor')
    parser.add_argument('--prompt', type=str, help='Specify a prompt file to use.')
    parser.add_argument('--input_dir', type=str, help='Specify input directory.')
    parser.add_argument('--output_dir', type=str, help='Specify output directory.')
    parser.add_argument('--model', type=str, default='gpt-4', help='Specify the OpenAI model to use.')
    parser.add_argument('--temperature', type=float, default=0.7, help='Set the temperature for the API.')
    parser.add_argument('--max_tokens', type=int, default=1500, help='Set the maximum tokens for the API.')
    # Add more arguments as needed

    args = parser.parse_args()

    # Load configuration
    config = Config()

    # Override config with CLI arguments if provided
    prompt_file = args.prompt if args.prompt else config.prompts_dir + 'standard_prompt.txt'
    input_dir = args.input_dir if args.input_dir else config.input_dir
    output_dir = args.output_dir if args.output_dir else config.output_dir
    model = args.model
    temperature = args.temperature
    max_tokens = args.max_tokens

    # Setup logger
    logger = setup_logger()

    # Ensure output directory exists
    ensure_directory(output_dir)

    # Initialize components
    prompt_manager = PromptManager(config.prompts_dir)
    file_handler = FileHandler(input_dir, output_dir)
    api_client = APIClient(config.openai.api_key, model, temperature, max_tokens)

    # Load prompt
    system_prompt = prompt_manager.load_prompt(prompt_file)

    # Process input files
    input_files = file_handler.list_input_files()
    for input_file in input_files:
        try:
            user_prompt = file_handler.read_file(input_file)
            response = api_client.send_prompt(system_prompt, user_prompt)
            output_filename = file_handler.write_output(input_file, response)
            logger.info(f"Processed {input_file} and saved to {output_filename}")
        except Exception as e:
            logger.error(f"Error processing {input_file}: {e}")
```

### Creating the `requirements.txt` File

```
openai
PyYAML
```

### Step-by-Step Installation Guide
1. **Open Command Prompt or PowerShell as Administrator:**
2. **Navigate to Your Project Directory:**
   ```bash
   cd path\to\your\project\directory
   ```
3. **Install the Package Using `pip`:**
   ```bash
   pip install .
   ```
4. **Verify Installation:**
   ```bash
   gpt-processer --help
   ```

### Optional: Install for a Single User Without Administrator Rights
```bash
pip install --user .
```

### Ensuring `gpt-processer` is Accessible from Any Path
1. **Locate the Python Scripts Directory:**
2. **Add to `PATH` If Not Already Present:**
3. **Restart Command Prompt or PowerShell:**

After these steps, your tool should be accessible from any directory via the command line.
